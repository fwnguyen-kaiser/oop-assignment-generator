difficulty: intermediate

# See configs/presets/beginner.yaml for the convention this file follows.

structure:
  classes:
    min: 4
    max: 6

oop:
  inheritance:
    enabled: true
    max_depth: 2
  abstraction:
    enabled: false
  interface:
    enabled: false
  composition:
    enabled: true
  aggregation:
    enabled: true
